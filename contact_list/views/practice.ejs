<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- <title><%= title %></title> -->
    <title><%=locals.title%></title>
  </head>
  <body>
    <h1>Playground</h1>

    <ul>
      <% for(let i=0;i<5;i++) {%>

      <li>
        <%= i %>
      </li>

      <%}%>
    </ul>

    <% if(true) { %>

    <h1>hey it is correct</h1>

    <% }else{%>
    <p>ohh false</p>

    <% } %>
  </body>
</html>
